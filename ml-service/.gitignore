.venv/
__pycache__/
*.pyc
.pytest_cache/
.env

# Training output, not source. Regenerated by `python -m training.train`.
# Checkpoints are tens of MB and are rebuilt from the exported dataset, so
# they are deliberately not committed — see docs/app_architecture.md §1p.
models/
lightning_logs/
