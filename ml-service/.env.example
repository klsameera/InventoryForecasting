ML_SERVICE_HOST=127.0.0.1
ML_SERVICE_PORT=8090

# Comma-separated bearer tokens this service will accept. Leave empty for
# local development (no auth enforced). See README.md before deploying
# anywhere reachable off localhost.
ML_SERVICE_API_TOKENS=
